# 3.4.3.4 Deployment Diagram — Shedulex Microservices

**Orchestration:** Docker Compose (`docker-compose.yml`)  
**Network:** `shedulex-net` (bridge)

## Deployment Diagram

```mermaid
flowchart TB
    subgraph Client["Client Tier"]
        Browser["Web Browser"]
        Vue["Vue 3 SPA<br/>Vite dev :5173<br/>Pinia + Vue Router + Axios"]
    end

    subgraph Gateway["API Gateway Tier"]
        Kong["Kong Gateway 3.6<br/>:8000 proxy / :8001 admin<br/>JWT · CORS · Rate Limiting<br/>gateway/kong.yml"]
    end

    subgraph Services["Microservices Tier — Flask + REST"]
        direction TB
        S1["auth-service :5001<br/>auth_db"]
        S2["timetable-engine :5002<br/>timetable_db<br/>★ GA Engine"]
        S3["adjustment-engine :5003<br/>adjustment_db<br/>★ LangGraph / Sora"]
        S4["notification-service :5004<br/>notification_db"]
        S5["calendar-service :5005<br/>calendar_db"]
        S6["document-service :5006<br/>document_db"]
        S7["analytics-service :5007<br/>analytics_db"]
        S8["audit-service :5008<br/>audit_db"]
    end

    subgraph Async["Async Processing Tier"]
        CW["notification-worker<br/>Celery worker"]
        CB["notification-beat<br/>Celery beat scheduler"]
    end

    subgraph Data["Data Tier"]
        PG["PostgreSQL 16<br/>:5543<br/>8 databases<br/>init_databases.sql"]
        Redis["Redis 7<br/>:6490<br/>Celery broker · pub/sub · cache"]
        Chroma["ChromaDB<br/>:8010<br/>Vector store for AI memory"]
    end

    subgraph External["External Services"]
        OpenAI["OpenAI API<br/>LLM for Sora agent"]
        Beem["Beem Africa API<br/>SMS delivery"]
        SMTP["SMTP Server<br/>Email delivery"]
    end

    Browser --> Vue
    Vue -->|HTTPS/HTTP REST| Kong

    Kong --> S1 & S2 & S3 & S4 & S5 & S6 & S7 & S8

    S1 --> PG
    S2 --> PG
    S3 --> PG
    S4 --> PG
    S5 --> PG
    S6 --> PG
    S7 --> PG
    S8 --> PG

    S1 --> Redis
    S2 --> Redis
    S3 --> Redis
    S4 --> Redis

    S3 --> Chroma
    S3 --> OpenAI
    S3 -->|internal HTTP| S2
    S6 -->|internal HTTP| S2
    S7 -->|internal HTTP| S2

    S4 --> CW
    S4 --> CB
    CW --> Redis
    CB --> Redis
    CW --> Beem
    CW --> SMTP

    style S2 fill:#e3f2fd,stroke:#1565c0
    style S3 fill:#f3e5f5,stroke:#7b1fa2
    style Kong fill:#fff3e0,stroke:#ef6c00
    style PG fill:#e8f5e9,stroke:#2e7d32
```

## Database-per-Service Mapping

```mermaid
flowchart LR
    subgraph Postgres["PostgreSQL 16 — shedulex-postgres"]
        auth_db[(auth_db)]
        timetable_db[(timetable_db)]
        adjustment_db[(adjustment_db)]
        notification_db[(notification_db)]
        calendar_db[(calendar_db)]
        document_db[(document_db)]
        analytics_db[(analytics_db)]
        audit_db[(audit_db)]
    end

    auth_svc[auth-service] --> auth_db
    tt_svc[timetable-engine] --> timetable_db
    adj_svc[adjustment-engine] --> adjustment_db
    notif_svc[notification-service] --> notification_db
    cal_svc[calendar-service] --> calendar_db
    doc_svc[document-service] --> document_db
    ana_svc[analytics-service] --> analytics_db
    aud_svc[audit-service] --> audit_db
```

## Container Reference

| Container | Image / Build | Host Port | Purpose |
|-----------|---------------|-----------|---------|
| shedulex-kong | kong:3.6-ubuntu | 8000, 8001 | API gateway |
| shedulex-auth | Backend/auth-service | 5001 | Authentication & RBAC |
| shedulex-timetable | Backend/timetable-engine | 5002 | GA generation & resources |
| shedulex-adjustment | Backend/adjustment-engine | 5003 | Sora AI assistant |
| shedulex-notification | Backend/notification-service | 5004 | SMS/email API |
| shedulex-notification-worker | Celery worker | — | Async delivery |
| shedulex-notification-beat | Celery beat | — | Scheduled reminders |
| shedulex-calendar | Backend/calendar-service | 5005 | Academic calendar |
| shedulex-document | Backend/document-service | 5006 | PDF/Excel/CSV export |
| shedulex-analytics | Backend/analytics-service | 5007 | KPI dashboards |
| shedulex-audit | Backend/audit-service | 5008 | Audit logging |
| shedulex-postgres | postgres:16-alpine | 5543 | Relational data |
| shedulex-redis | redis:7-alpine | 6490 | Cache & message broker |
| shedulex-chromadb | chromadb/chroma | 8010 | Vector embeddings |

## Kong Route Summary

| Path prefix | Upstream service | JWT required |
|-------------|------------------|--------------|
| `/api/v1/auth`, `/api/v1/users` | auth-service | Partial (login public) |
| `/api/v1/timetable`, `/api/v1/courses`, `/api/v1/rooms`, … | timetable-engine | Yes |
| `/api/v1/adjustments` | adjustment-engine | Yes |
| `/api/v1/notifications` | notification-service | Yes |
| `/api/v1/calendar` | calendar-service | Yes |
| `/api/v1/documents` | document-service | Yes |
| `/api/v1/analytics` | analytics-service | Yes |
| `/api/v1/audit` | audit-service | Yes |

## Figure Caption (for report)

> **Figure 3.4 Deployment Diagram** — Shedulex microservices deployment on Docker Compose showing the Vue frontend, Kong API gateway, eight Flask microservices, PostgreSQL database-per-service pattern, Redis, ChromaDB, and Celery workers.
